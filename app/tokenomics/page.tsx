'use client';

import TokenomicsSimulator from '../../TokenomicsUI';
//  ↑ path points up twice (..) then to the file you already have.
//    If your file is called TokenomicsUI.tsx and sits in the repo root,
//    this import is correct.

export default function Page() {
  return <TokenomicsSimulator />;
}
